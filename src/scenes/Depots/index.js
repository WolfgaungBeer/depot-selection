import React from 'react';

const propTypes = {
};

const defaultProps = {
};

const Depots = () => (
    <div>
        depots
    </div>
);

Depots.propTypes = propTypes;
Depots.defaultProps = defaultProps;

export default Depots;
